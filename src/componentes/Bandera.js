export const Bandera = ({message}) => {
    return (
        <div style={{fontSize: "1.1em",height: "70vh",width: "100%",display: "flex", justifyContent: "center",alignItems: "center"}}>
            {message}
        </div>
    )
}
